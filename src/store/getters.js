export default {
  count: function count(state) {
    console.low(state);
    // return state.header.count;
  },
  POItypes: function POItypes(state) {
    console.log(state);
    // return state.header.POItypes;
  },
};
